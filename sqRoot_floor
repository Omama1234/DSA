#include<bits/stdc++.h>

using namespace std;


int main()
{
    int n;
    cout<<"Enter the value of n ->";
    cin>>n;

    int i=1;
    while(i*i<=n)
    {
        i++;
    }
    cout<<"floor of sqrt("<<n<<") -> "<<(i-1)<<endl;
}
